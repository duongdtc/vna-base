import { ColorLight } from '@theme/color';
import { HairlineWidth } from '@vna-base/utils';

export const SIZE = 30;

export const ACTIVE_COLOR = ColorLight.primary500;

export const UN_ACTIVE_COLOR = ColorLight.neutral500;

export const STROKE_WIDTH = HairlineWidth * 5;
